namespace WealthOS.Domain.Notifications.Enums;

/// <summary>
/// Lifecycle status for a user reminder.
/// </summary>
public enum ReminderStatus
{
    Active = 0,
    Completed = 1,
    Dismissed = 2,
    Cancelled = 3,
}
