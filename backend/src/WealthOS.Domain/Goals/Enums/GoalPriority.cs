namespace WealthOS.Domain.Goals.Enums;

/// <summary>
/// Relative priority used for ordering and planning.
/// </summary>
public enum GoalPriority
{
    Low = 0,
    Medium = 1,
    High = 2,
    Critical = 3,
}
