# Copy to .env.production before `npm run build` (or set the same vars in CI).
# Production builds also force api mode unless VITE_ALLOW_MOCK_PROD=true.

VITE_API_MODE=api
VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_VERSION=1
