# Copy to .env.production before `npm run build` (or set the same vars in CI).
# Production image builds also inject these via Docker build args.

VITE_API_MODE=api
VITE_API_BASE_URL=https://api.wealthos.devenlight.com
VITE_API_VERSION=1
